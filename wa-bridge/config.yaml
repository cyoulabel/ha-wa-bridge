name: "WhatsApp Bridge"
version: "2.4.2"
slug: "ha_wa_bridge"
description: "WhatsApp Web Bridge for Home Assistant"
url: "https://github.com/cyoulabel/ha-wa-bridge"
arch:
  - aarch64
  - amd64
  - armv7
startup: application
boot: auto
map:
  - config:rw
options:
  detect_own_messages: false
  incoming_messages_mode: "all"
  incoming_message_log_level: "FULL"
  allowed_groups: []
  allowed_numbers: []
  media_dir: "/config/shell_scripts/wa_media"
schema:
  detect_own_messages: bool
  incoming_messages_mode: "match(all|disabled|groups_only|numbers_only)"
  incoming_message_log_level: "match(FULL|COMPACT|NONE)"
  allowed_groups: [str]
  allowed_numbers: [str]
  media_dir: str
ports:
  3000/tcp: 3000
